language: node_js
node_js:
    - "node"
    - "7"
cache:
    directories:
        - "node_modules"

before_script:
   - npm install

script: npm run build

deploy:
    provider: pages
    skip_cleanup: true,
    github_token: $GITHUB_TOKEN
    on:
        branch: gh-pages

